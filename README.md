# 💻 JavaScript Auto Typing Text Animation Effect

A smooth and interactive **auto typing text animation** created using **HTML**, **CSS**, and **JavaScript**.  
This effect simulates the typing and erasing of multiple words — giving a dynamic and lively feel to web pages.

---

## 🌐 Live Demo

👉 [View the Project on GitHub Pages](https://maheshkakarla2003.github.io/CSS-JavaScript-Auto-Typing-Text-Animation-Effect/)

*(Click the link above to see the typing animation in action.)*

---

## 🧱 Technologies Used

- **HTML5** – For basic structure  
- **CSS3** – For styling, colors, and cursor blinking animation  
- **JavaScript (ES6)** – For controlling the typing and erasing logic

---

## 💡 What This Project Demonstrates

- How to create a **typing and erasing animation** effect  
- Using **JavaScript timing functions** (`setTimeout`) to manage animation speed  
- Implementing a **looping text cycle**  
- Adding a **blinking cursor** using CSS animations  
- Smooth and reusable logic for dynamic word transitions

---

## ⚙️ How It Works

1. The `.typed-text` element displays one character at a time from the `words` array.  
2. After a word finishes typing, it waits briefly, then erases the text.  
3. The process repeats infinitely, cycling through all the words.  
4. The `.cursor` element blinks continuously for a realistic typing effect.

---

## 🧩 Key JavaScript Variables

| Variable | Purpose |
|-----------|----------|
| `words` | Stores the array of words to be displayed |
| `typingSpeed` | Controls typing speed (in milliseconds) |
| `erasingSpeed` | Controls how fast text is erased |
| `delayBetweenWords` | Adds delay between typing and erasing cycles |

---

## 🎨 Key CSS Features

| Selector | Description |
|-----------|-------------|
| `.cursor` | Creates a blinking typing cursor |
| `.typed-text` | Holds the dynamic animated text |
| `.js` | Highlights the static word “JavaScript” in yellow |
| `@keyframes blink` | Controls the blinking animation timing |

---

## 🧠 Concept Highlight

This project demonstrates how **simple DOM manipulation** and **timing logic** can produce elegant text animations.  
It’s a great beginner-friendly example of **JavaScript interactivity** combined with clean **CSS visuals**.

---

